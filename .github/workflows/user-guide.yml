# this workflow trigger conversion of dsrqst.usg, if it is changed, into section*.rst in repository
# NCAR/gdex-docs-dsrqst
#
on:
  pull_request:
    paths:
      - 'src/rda_python_dsrqst/dsrqst.usg'

jobs:
  trigger_conversion:
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.DSRQST_USG }}
          repository: NCAR/gdex-docs-dsrqst
          event-type: trigger-conversion
          client-payload: '{"branch":"${{ github.event.pull_request.head.ref }}"}'
          
